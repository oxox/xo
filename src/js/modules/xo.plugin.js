//plugin base module
XO('plugin',function($,C){
    var base = {

    };
    /**
     * 在指定的view上面查找data-plugin属性，用data-plugin_params属性作为初始化配置对象进行插件的初始化。
     * @param {Object} view ox.view object
     */
    this.applyToView = function(view){
        var $el = view.$el;
        //TODO:
    };

    this.define = function(){
        
    };

});